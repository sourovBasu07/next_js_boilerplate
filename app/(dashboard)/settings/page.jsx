// Parallel routing system by adding multiple slots(pages) withng a single page
// Changing or sub-navigating in one slot does not re-render the full page 

// Slots does not affect the routes 

import React from 'react'

const Settings = () => {
    return (
        <div>Settings Parallel Routes</div>
    )
}

export default Settings